# experiment_name: "disrupt_model"
# run_name: "dataset_2022-10-12"
# data_source: "/zfsauton2/home/youngsec/research/fusion/disruption/model_data_file_fixed.h5"
experiment_name: "dataset_2022-10-26"
run_name: "classification_sigmoid_bias_500"
data_source: "/home/scratch/youngsec/rs/fusion/disruption/datasets/scan_sigmoid_bias/scan_sigmoid_bias_500.h5"
seed: 1
logger: tensorboard
normalization: "standardize_input"
defaults:
    - trainer: pl_trainer
    - data_module: classification_data_module
    - early_stopping: default
    - model: classifier

trainer:
    max_epochs: 3000
    devices: 1
    accelerator: 'gpu'
data_module:
    batch_size: 512
    val_proportion: 0.0
    test_proportion: 0.0
    num_workers: 16
save_dir: "/zfsauton2/home/youngsec/research/fusion/disruption/train_runs"
gpus: 1

# modifications 1: make the network bigger
model:
    num_layers: 5
    layer_size: 300
    hard_labels: 0

      # hydra:
      #     job:
      #         name: ${name}
      #     run:
      #         dir: results/${name}/${now:%Y-%m-%d-%H-%M}
