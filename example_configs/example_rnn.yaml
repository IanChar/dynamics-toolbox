name: "example_rnn"
save_path: "trained_models"
data_source: "data/pendulum_1k.hdf5"
seed: 1
logger: tensorboard
normalization: "standardize"
cuda_device: null
defaults:
    - model: rpnn
    - trainer: pl_trainer
    - data_module: sequential_data_module
    - early_stopping: default
hydra:
    job:
        name: ${name}
    run:
        dir: ${save_path}/${name}
