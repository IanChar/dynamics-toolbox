name: "test_weather_rnn"
save_path: "trained_models"
data_source: "data/weather_feature_1.h5"
seed: 1
logger: tensorboard
normalization: "standardize"
cuda_device: null
defaults:
    - model: rnn
    - trainer: pl_trainer
    - data_module: timeseries_data_module
    - early_stopping: default
#hydra:
#    job:
#        name: ${name}
#    run:
#        dir: ${save_path}/${name}
